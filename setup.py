from setuptools import setup, find_packages

setup(
    name="metrics_calculate",  # Your package name
    version="0.1.0",
    packages=find_packages(include=["metrics_calculate"]),
    install_requires=[],  # Add any dependencies your package needs
    description="A short description of your metrics_calculate package",
    url="https://github.com/ghbihuy123/package_learning",  # Your project URL
    python_requires=">=3.6",  # Specify the minimum Python version
)